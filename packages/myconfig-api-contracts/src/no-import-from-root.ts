/*
 * This is the root entry point for the Contracts package: it should not be used.
 *
 * The values package and SDK should instead import from a subpath like `/v1`:
 *    import { v1Beverage_payloadSchema } from "@spautz/myconfig-api-contracts/v1";
 */

throw new Error("Do not import from root, use a subpath like '/v1'");

export {};
